# Foyer — tutorial 1, starting point

The tree Duet Tutorial 1 opens: a Kotlin Multiplatform project that declares
no feature yet. The tutorial text lives at
<https://docs.modaal.dev/tutorials/duet-01-first-feature>; this README is a
pointer, not a copy.

```sh
tools/duet verify
```

The command above is green before the first edit. This tree carries no
deliberately failing test: Tutorial 1's closing exercise breaks the feature
you write and watches a recording catch it.
