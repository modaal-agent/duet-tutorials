# Foyer — tutorial 3, starting point

The tree Duet Tutorial 3 opens: Tutorial 2's finished tree — the `splash`
feature, the Apple boundary, the Swift shell, the SwiftUI app and the Compose
app — plus one deliberately failing test, the tutorial's closing exercise.
The tutorial text lives at
<https://docs.modaal.dev/tutorials/duet-03-composing-features>; this README
is a pointer, not a copy.

```sh
tools/duet verify
```

The command above is green before the first edit, apart from the one
deliberately failing test the tutorial's closing exercise asks you to make
pass (`Tutorial3ExerciseEditNameChainTest`).
