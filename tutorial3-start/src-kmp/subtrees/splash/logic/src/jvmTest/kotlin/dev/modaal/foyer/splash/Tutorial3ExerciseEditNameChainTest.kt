// Copyright (c) 2026 Modaal.dev
// Licensed under the MIT License. See LICENSE file for details.

package dev.modaal.foyer.splash

import kotlin.test.Test
import kotlin.test.fail

/**
 * Tutorial 3's closing exercise: the `chain-profile-editname` chain scenario,
 * the `Saved` delegate event climbing from `editName` through `account` to
 * `profile` as `NameChanged`. This file fails until you replace it with the
 * chain scenario in the module Tutorial 3 creates for it; it sits in the
 * splash module only because no other test module exists at this step.
 */
class Tutorial3ExerciseEditNameChainTest {
  @Test
  fun savedClimbsToProfileAsNameChanged() {
    fail("Tutorial 3 exercise: record the chain-profile-editname scenario")
  }
}
