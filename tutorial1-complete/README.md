# Foyer — tutorial 1, complete

The tree Duet Tutorial 1 ends with: the `splash` feature in Kotlin, its
scenario, the four recordings under `parity/fixtures/`, the golden replays,
and the test-clock suite for the safety net. No UI yet. The tutorial text
lives at <https://docs.modaal.dev/tutorials/duet-01-first-feature>; this
README is a pointer, not a copy.

```sh
tools/duet verify
```

The command above is green as committed.
