# Foyer — tutorial 2, starting point

The tree Duet Tutorial 2 opens: Tutorial 1's finished tree — the `splash`
feature in Kotlin, its recordings and its test-clock suite — plus one
deliberately failing test, the tutorial's closing exercise. No UI yet. The
tutorial text lives at <https://docs.modaal.dev/tutorials/duet-02-two-apps>;
this README is a pointer, not a copy.

```sh
tools/duet verify
```

The command above is green before the first edit, apart from the one
deliberately failing test the tutorial's closing exercise asks you to make
pass (`Tutorial2ExerciseMountBracketTest`).
