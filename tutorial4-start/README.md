# Foyer — tutorial 4, starting point

The tree Duet Tutorial 4 opens: Tutorial 3's finished tree — the feature
tree over the splash, the four ports and their mock services, the
composition triples on both sides, the chain recordings — plus one
deliberately failing test, the tutorial's closing exercise. The tutorial
text lives at <https://docs.modaal.dev/tutorials/duet-04-workers>; this
README is a pointer, not a copy.

```sh
tools/duet verify
```

The command above is green before the first edit, apart from the one
deliberately failing test the tutorial's closing exercise asks you to make
pass (`Tutorial4ExerciseSessionExpiryTest`).
