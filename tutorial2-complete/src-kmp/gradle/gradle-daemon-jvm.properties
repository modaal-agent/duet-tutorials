# The JVM the Gradle daemon runs on, and with it every worker it starts (the
# KSP workers included). One number for the daemon, the workers and the
# modules' declared toolchain, whatever JAVA_HOME the caller has.
toolchainVersion=25
